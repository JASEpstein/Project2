{{!-- <br>
<br>

{{! SHOPPING CART }}
<div class="infoSection"></div>
<div>
    <h1 class="infoTitle">Shopping Cart</h1>
    <div class="infoSection"></div>
</div>

<div class="container cartContainer">
  <div class="row cart-row">  
    <div class="col-md-3 col-sm-6 cartItem">
      <div class="thumbnail">
        <img src="/styles/assets/product/mediumbox.png" alt="One Time">
        </a>
        <div class="caption">
          <h3>One Time</h3>
          <p class="price">$29.99</p>
          <form method="POST" action="/cart/id/?_method=PUT">
            <input type="hidden" value="id" name="itemId">
            <div class="form-group">
              <label for="quantity">Qty</label>
              <select class="form-control cartItem-dropdown" id="quantity" name="quantity">
                <option selected>1</option>
                <option>1</option>
                <option>2</option>
                <option>3</option>
              </select>
              <button class="btn btn-default noPad update" type="submit" value="Submit">Update Quantity</button>
            </div>
          </form>
          <form method="POST" action="/cart/id?_method=DELETE">
            <div class="form-group">
              <div class="btn-group"> 
                <button class="btn btn-danger remove" type="submit" value="Submit">Remove</button>
              </div>
              <input type="hidden" value="id" name="quantity">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

   
  <div class="row">
    <div class="col-md-12">
      <a href="/order" class="btn btn-primary checkout">Checkout</a>
      <a href="/" class="btn btn-primary ">Continue Shopping</a>
    </div>
  </div>
</div> --}}

{{!===================================================================}}
{{!===================================================================}}
{{!===================================================================}}
{{!===================================================================}}
<br>
<br>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2>Shopping Cart</h2>
    </div>
  </div>

  {{#if Subscriptions}}
   
  {{#grouped_each 4 Subscriptions}}
  <div class="container cartContainer">
  <div class="row cart-row">  
    {{#each this}}
    <div class="col-md-3 col-sm-6 product">
      <div class="thumbnail">
        <img src="{{Subscriptions.img}}" alt="{{Subscriptions.name}}">
        </a>
        <div class="caption">
          <h3>{{Subscription.name}}</h3>
          <p class="price">${{Subscriptions.price}}</p>
          <form method="POST" action="/cart/{{Subscriptions.id}}/?_method=PUT">
            <input type="hidden" value={{Subscriptions.id}} name="itemId">
            <div class="form-group">
              <label for="quantity">Qty</label>
              <select class="form-control product-dropdown" id="quantity" name="quantity">
                <option selected>{{quantity}}</option>
                <option>1</option>
                <option>2</option>
                <option>3</option>
              </select>
              <button class="btn btn-default noPad update" type="submit" value="Submit">Update Quantity</button>
            </div>
          </form>
          <form method="POST" action="/cart/{{id}}?_method=DELETE">
            <div class="form-group">
              <div class="btn-group"> 
                <button class="btn btn-danger remove" type="submit" value="Submit">Remove</button>
              </div>
              <input type="hidden" value={{quantity}} name="quantity">
            </div>
          </form>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  </div>
  {{/grouped_each}}

  {{else}}
  <p>There are no items in your cart.</p>
  {{/if }}
   
  <div class="row">
    <div class="col-md-12">
    {{#if Subscriptions}}
      <a href="/order" class="btn btn-primary checkout">Checkout</a>
    {{else}}
      <a href="/" class="btn btn-primary">Lets go shopping!</a>
    {{/if}}
    </div>
  </div>
</div>
